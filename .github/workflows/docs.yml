name: Publish documentation

on:
  push:
    branches:
      - master
    tags:
      - v[0-9]+.[0-9]+.[0-9]+*

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions@setup-node@v1
      - run: npm install --engine-strict --no-lockfile
      - run: test/ci/publish_docs.sh
